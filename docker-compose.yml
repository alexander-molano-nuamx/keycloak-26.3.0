services:
  keycloak:
    build: .
    container_name: keycloak
    ports:
      - "8080:8080"
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    volumes:
      - ./themes:/opt/keycloak/themes
    command:
      - start-dev
